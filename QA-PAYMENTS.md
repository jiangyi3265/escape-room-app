# 两字进度与收款验收

## 规则测试

`npm run test:payments` 覆盖 10 组检查：金额精确到分和非法输入、前置节点和在职权限、四渠道混合收款、线上排除与跨日、重复提交、流程回退后恢复旧记录、取消/删除后保留收款、纯线上收款、12 个两字标签与视频分支、旧数据迁移和台账重载。

`npm run test:store-config` 原有 11 组配置和维修测试仍通过。

## 浏览器实测清单

测试使用独立 H5 浏览器上下文，不修改初始数据或用户设备数据。

- 390×844 订单列表：12 个两字标签分为两行；已完成灰色、当前节点描边、未开始高亮。
- 创建订单，依次点击主题开始、开场复位：收钱节点显示四个渠道输入框。
- 不填金额提交：显示错误，不能跳过收钱；修改输入时清除旧错误。
- 填入微信 100.10、支付宝 50.20、现金 20、线上 300：本单总计 470.30；取消确认保留表单，确认后推进至带场。
- 店长当日微信 100.10、支付宝 50.20、现金 20，三项合计 170.30；刷新后保持。
- 收钱后依次完成带场、入场、开始游戏、游戏结束、收场复位、不要拍照、不要视频：订单结束，12 个标签均完成。
- 店长回退至开场复位，恢复原收款节点：不重复记账；删除该测试订单后，已收 170.30 仍保留。
- 另一订单只填写线上 300：可正常完成收钱，三项日合计仍为 170.30。
- 360×800 视口检查收款表单与店长日合计，无横向溢出，浏览器没有页面脚本错误。

## 边界与未验收部分

- 今日按设备本地日期计算；跨日规则有自动测试，页面时钟每秒刷新日期依赖。
- 金额用整数分存储和累加，不进行真实支付，不自动退款，不提供历史报表。
- H5 与微信小程序均编译通过；尚未做微信开发者工具、键盘弹起或真机验收。
- 多人同步、服务端权限和收款防重复需接入后端后再验收，当前仍为本地演示。
