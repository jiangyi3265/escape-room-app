# 已完成订单员工端隐藏

## 自动检查

运行 `npm run test:order-visibility`，9 组检查覆盖：员工全部/进行中/待剪辑筛选不泄露完成订单、店长历史保留、直接打开与编辑拦截、不要视频完成后关闭详情、要视频等待剪辑完成、切换角色重置已完成筛选、过期编辑表单禁止保存、首页数量更新与店长回退后恢复可见、刷新与本地记录保留。

现有 `test:payments` 10 组、`test:store-config` 11 组回归通过。H5、微信小程序编译通过。

## H5 实际交互

- 独立浏览器上下文，390×844 和 360×800；不改用户设备数据。
- 员工进入待剪辑订单，点击剪辑完毕：详情自动关闭，列表不再出现该订单。店长的已完成筛选仍可打开并查看完整记录。
- 选择不要视频：取消确认时仍可见；确认完成时立即隐藏；刷新后仍隐藏。
- 选择要视频后，订单仍可在待剪辑中打开，直至点击剪辑完毕。
- 完成所有测试订单后，员工列表显示空状态，首页待完成数量为 0，无已完成订单卡片。
- 店长从已完成订单回退流程，切回员工端后待处理订单重新出现。
- 切换角色时不会残留员工端没有的已完成筛选；新列表和首页提示无横向溢出，浏览器未出现页面脚本错误。

## 范围

只调整订单可见性，不删除历史记录，不修改取消订单规则，不删除已有通知。当前是本地存储与演示角色切换；正式员工数据访问限制仍需后端校验。本次未做微信开发者工具或真机验收。
