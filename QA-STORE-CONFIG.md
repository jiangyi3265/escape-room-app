# 主题、积分设置与维修验收

## 覆盖清单

本次以实际新增功能为范围，不将 H5 测试等同于微信真机或多人联机验收。

| 用户可见行为 | 验证方式与结果 |
| --- | --- |
| 员工与店长都可打开维修；店长独有积分设置和主题管理 | H5 390px / 360px 视口点击入口和切换身份；规则测试拒绝员工修改配置 |
| 12 个实际主题出现在新订单和维修表单 | 核对列表文本与初始数据测试 |
| 维修必须选择主题并填写问题 | H5 空备注提交显示错误；规则测试缺失主题、空白备注和超长备注 |
| 员工登记、店长修好；店长登记、员工修好 | H5 两个方向均点击完成，记录对应登记人与完成时间 |
| 待维修/已完成筛选、返回、取消表单 | H5 通过，已完成记录不再显示完成按钮 |
| 已完成不可重复提交，停用员工不能登记 | 业务规则测试通过 |
| 删除主题仍可完成该主题已有维修 | H5 新建测试主题、登记、删除、切换员工后完成 |
| 预设积分可修改并实际到账 | H5 将普通任务改为 9 分，员工领取后明细 +9，总积分由 186 变为 195 |
| 待审核不受后续改分影响 | H5 先提交 4 分打扫任务，再改配置为 12 分，审核仍发放 4 分 |
| 任务名称、非负整数校验、历史积分保留 | 规则测试通过，包括 0 分与无效数值 |
| 主题新增、改名、删除 | H5 创建测试主题、改名；新订单选项同步；删除后剩余 12 个实际主题 |
| 重复主题名称不允许 | H5 再新增“开学悸”显示“主题名称已存在”；规则测试覆盖空白名称 |
| 开始节点显示具体主题名称和状态 | H5 创建新主题订单、点击开始，显示“《测试主题新版》· 已开始”及操作人时间 |
| 配置、维修和软删除状态刷新保留 | H5 刷新复查；业务规则测试 v2 → v3 迁移和 v3 保存重载 |
| 全部主题删除后的空状态不自动重新填入种子数据 | 规则测试通过；页面提供先由店长添加主题的说明 |
| 暗色样式、窄屏、长主题名与记录显示 | 检查 390×844 与 360×800 截图，关键表单/按钮无横向溢出，页面无 JS 运行错误 |

## 可复现命令

```bash
npm run test:store-config
npm run build:h5
npm run build:mp-weixin
```

规则脚本含 11 组检查，测试不写入真实小程序本地数据。浏览器交互使用独立浏览器上下文，测试名称和改分不会写入项目初始数据。

## 限制

- 当前是单设备本地持久化版，角色切换为演示模式。多人同时使用、登录权限、云端维修和配置同步尚需后端接入。
- 微信小程序仅完成编译验证，尚未使用微信开发者工具或真机验收；应重点复查安全区、原生输入框、键盘和滚动行为。
